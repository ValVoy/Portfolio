# Portfolio — Next.js App

Voir le README principal à la racine du repo pour la documentation complète.

## Lancer en local

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

Ouvrir [http://localhost:3000](http://localhost:3000).
